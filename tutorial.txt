[obs]
flex 1 para ocupar o espaço disponivel da tela.
SafeAreaView só suporta IOS
lembrar de instalar plugin do react hooks
https://www.npmjs.com/package/eslint-plugin-react-hooks
styled components precisa ser importado de  'styled-components/native'
${({ variant, theme }) => variants[variant](theme)}; //short hand syntax to take the properties of the props object (destructuring)
//add properties to json example
  const newResult = camelize(result);
  newResult.someExtraProperty = 'new'; //add properties
//----------
[commands]
adb devices
yarn create @eslint/config
yarn add react-native-paper
yarn add -D eslint-plugin-prettier@latest
expo install expo-font @expo-google-fonts/inter
expo install react-native-svg
yarn add camelize
